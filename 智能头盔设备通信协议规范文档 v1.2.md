# 智能头盔设备通信协议规范文档 v1.2

---

## 版本说明

v1.2 基于 v1.1 扩展，保持向后兼容。

本版本新增人体健康监测数据：

1. 人体温度传感器：用于采集人体体表/体温数据。
2. 心率传感器：通过 IR LED 追踪心跳频率，输出 HR。
3. 血氧饱和度传感器：基于红光和红外光吸收差异计算 SpO2。

v1.2 只新增字段，不删除 v1.1 已有字段。服务端如果暂不支持新增字段，应忽略未知字段。

---

## 目录

1. [字段定义表](#1-字段定义表)
2. [数据发送与传输](#2-数据发送与传输)
3. [MQTT 传输协议](#3-mqtt-传输协议)
4. [心跳包与离线检测](#4-心跳包与离线检测)
5. [单位规范](#5-单位规范)
6. [状态码规范](#6-状态码规范)
7. [异常与缺失值处理](#7-异常与缺失值处理)
8. [完整报文示例](#8-完整报文示例)
9. [版本兼容策略](#9-版本兼容策略)

---

## 1. 字段定义表

### 1.1 基础字段

| 字段 | 类型 | 单位 | 说明 |
| --- | --- | --- | --- |
| version | string | - | 协议版本号，当前为 `"1.2"` |
| device_id | string | - | 设备唯一 ID |
| timestamp | int | 秒 | Unix 时间戳 |
| msg_type | string | - | 消息类型标识：`data` / `heartbeat` / `event` |

### 1.2 IMU 数据字段

| 字段 | 类型 | 单位 | 说明 |
| --- | --- | --- | --- |
| ax | float | g | X 轴加速度 |
| ay | float | g | Y 轴加速度 |
| az | float | g | Z 轴加速度 |
| gx | float | °/s | X 轴角速度 |
| gy | float | °/s | Y 轴角速度 |
| gz | float | °/s | Z 轴角速度 |

### 1.3 GPS 数据字段

| 字段 | 类型 | 单位/范围 | 说明 |
| --- | --- | --- | --- |
| lat | float | 度 | 纬度，WGS84 |
| lon | float | 度 | 经度，WGS84 |
| alt | float | m | 海拔高度 |
| speed | float | m/s | 地面速度 |
| course | float | ° | 航向角，0-360 |
| sat | int | >= 0 | 可见卫星数 |
| hdop | float | 越小越好 | 水平精度因子 |
| fix | int | 0/1/2 | 定位状态 |

fix 定义：

| 值 | 含义 |
| --- | --- |
| 0 | 无定位 |
| 1 | 2D 定位 |
| 2 | 3D 定位 |

### 1.4 环境数据 env

| 字段 | 类型 | 单位 | 说明 |
| --- | --- | --- | --- |
| temp | float | ℃ | 环境温度 |
| hum | float | % | 环境湿度 |
| light | int | lux/raw | 光照强度 |

### 1.5 人体健康数据 vital

v1.2 新增 `vital` 对象，用于承载人体温度、心率和血氧数据。

| 字段 | 类型 | 单位/范围 | 说明 |
| --- | --- | --- | --- |
| body_temp | float | ℃ | 人体温度，可为体表温度或算法换算体温 |
| hr | int | bpm | 心率，HR / Heart Rate；MAX30100 示例库中 `0` 表示无效 |
| spo2 | int | % | 血氧饱和度，SpO2；MAX30100 示例库中 `0` 表示无效 |
| hr_valid | int | 0/1 | 心率数据是否有效，协议层字段 |
| spo2_valid | int | 0/1 | 血氧数据是否有效，协议层字段 |

说明：

1. `body_temp` 表示人体相关温度，不等同于 `env.temp` 环境温度。
2. 心率 `hr` 由 IR LED PPG 信号计算。
3. 血氧 `spo2` 基于红光和红外光吸收差异计算。
4. MAX30100 常见 Arduino 示例库对外主要提供 `getHeartRate()`、`getSpO2()` 和 raw `ir/red`；其中 HR 或 SpO2 为 `0` 表示该值无效。

### 1.6 事件字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| sos | int | 1 = 用户触发 SOS |
| fall_detected | int | 1 = 检测到跌倒 |
| helmet_on | int | 头盔佩戴状态变化 |

### 1.7 设备状态 device

| 字段 | 类型 | 单位 | 说明 |
| --- | --- | --- | --- |
| battery | int | % | 电量，0-100 |
| signal | int | 0-5 | 4G 信号强度 |
| work_time | int | 秒 | 当前连续工作时长 |
| helmet_on | int | - | 1 = 已佩戴，0 = 已脱下 |

### 1.8 预警状态 alerts

| 字段名 | 类型 | 含义 | 触发来源 |
| --- | --- | --- | --- |
| collision_alert | int | 碰撞风险预警 | IMU 加速度/角速度突变 |
| heat_alert | int | 环境高温中暑风险 | 环境温湿度 + 活动强度 + work_time |
| fatigue_alert | int | 疲劳风险预警 | IMU 行为模式 + work_time + 生命体征 |
| sos_alert | int | SOS 紧急求救 | 手动按键 / 系统自动触发 |
| radar_alert | int | 障碍/接近风险 | 超声波/雷达传感器 |
| body_temp_alert | int | 人体温度异常 | 人体温度传感器 |
| hr_alert | int | 心率异常 | PPG 心率传感器 |
| spo2_alert | int | 血氧异常 | PPG 血氧传感器 |

新增健康预警阈值参考：

| 预警类型 | 触发条件示例 |
| --- | --- |
| body_temp_alert | body_temp >= 37.3℃ 触发 1；>= 38.0℃ 触发 2；>= 39.0℃ 触发 3 |
| hr_alert | 静息/低活动状态 HR < 50 或 HR > 120 触发 1；HR > 140 触发 2；HR > 160 触发 3 |
| spo2_alert | SpO2 < 95 触发 1；< 90 触发 2；< 85 触发 3 |

> 阈值需要结合实际作业场景、佩戴位置、传感器精度和算法稳定性调整。

---

## 2. 数据发送与传输

### 2.1 数据发送频率

- IMU 采样频率：100ms，10Hz。
- 常规数据上报频率：1Hz，每 1 秒打包上报一次。
- 心跳包频率：每 30 秒上报一次。
- 事件触发消息：实时上报，不受 1Hz 限制。
- 心率/血氧建议内部采样频率高于 25Hz，但上报字段按 1Hz 输出算法结果。

### 2.2 数据大小控制

- 单条 JSON 建议不超过 768 字节。
- 浮点数保留 2 位小数。
- 字段为空或传感器不可用时使用 `null`。
- `vital` 对象可选；未接人体传感器时可不上传该对象，或上传对象内字段为 `null`。

---

## 3. MQTT 传输协议

### 3.1 Broker 配置

| 参数 | 说明 |
| --- | --- |
| 协议 | MQTT v3.1.1 或 v5.0 |
| 端口 | 1883 TCP / 8883 TLS，或项目实际分配端口 |
| 认证方式 | Username + Password / 证书双向认证 |
| Keep Alive | 60 秒 |
| Clean Session | false，保持会话，支持离线消息 |

### 3.2 Topic 命名规则

```text
helmet/{device_id}/{msg_type}
```

| Topic | 方向 | 说明 |
| --- | --- | --- |
| `helmet/{device_id}/data` | 设备 -> 服务端 | 常规传感器数据 |
| `helmet/{device_id}/heartbeat` | 设备 -> 服务端 | 心跳包 |
| `helmet/{device_id}/event` | 设备 -> 服务端 | 事件/预警触发消息 |
| `helmet/{device_id}/cmd` | 服务端 -> 设备 | 下行指令 |
| `helmet/{device_id}/status` | 双向 | 设备在线状态 |

### 3.3 QoS 等级

| 消息类型 | QoS 等级 | 说明 |
| --- | --- | --- |
| 常规数据 data | QoS 0 | 允许丢包，减少带宽消耗 |
| 心跳包 heartbeat | QoS 1 | 至少送达一次 |
| 事件/预警 event | QoS 1 | 至少送达一次 |
| SOS event | QoS 2 | 精确一次，最高可靠性 |
| 下行指令 cmd | QoS 1 | 至少送达一次 |

### 3.4 遗嘱消息

Topic：

```text
helmet/{device_id}/status
```

Payload：

```json
{
  "device_id": "HLM-00123",
  "online": 0,
  "timestamp": 1713600000
}
```

### 3.5 断网缓存与重传

- 设备断网期间，事件和预警消息缓存至本地存储，建议 FIFO 队列最大 200 条。
- 恢复网络后按时间顺序批量上报，payload 中保留原始 `timestamp`。
- 常规数据断网期间可丢弃，不强制缓存。

---

## 4. 心跳包与离线检测

### 4.1 心跳包格式

Topic：

```text
helmet/{device_id}/heartbeat
```

Payload：

```json
{
  "version": "1.2",
  "device_id": "HLM-00123",
  "msg_type": "heartbeat",
  "timestamp": 1713600030,
  "battery": 85,
  "signal": 4,
  "helmet_on": 1
}
```

### 4.2 离线检测机制

| 阶段 | 时间阈值 | 服务端动作 |
| --- | --- | --- |
| 超时预警 | 超过 60 秒未收到心跳 | 标记为连接不稳定，推送告警 |
| 判定离线 | 超过 90 秒未收到心跳 | 标记设备离线，触发离线通知 |
| 长时间离线 | 超过 10 分钟 | 升级告警级别，通知管理员 |

---

## 5. 单位规范

### 5.1 时间

| 项目 | 单位 | 说明 |
| --- | --- | --- |
| timestamp | 秒 | Unix 时间戳 |
| work_time | 秒 | 连续工作时长 |

### 5.2 运动数据

| 字段 | 单位 | 说明 |
| --- | --- | --- |
| ax / ay / az | g | 三轴加速度 |
| gx / gy / gz | °/s | 三轴角速度 |

### 5.3 环境数据

| 字段 | 单位 | 说明 |
| --- | --- | --- |
| temp | ℃ | 环境温度 |
| hum | % | 相对湿度 |
| light | lux/raw | 光照强度 |

### 5.4 GPS 数据

| 字段 | 单位 | 说明 |
| --- | --- | --- |
| lat / lon | 度 | WGS84 坐标 |
| alt | m | 海拔 |
| speed | m/s | 地面速度 |
| course | ° | 航向角 |
| hdop | 无量纲 | 精度因子 |

### 5.5 人体健康数据

| 字段 | 单位 | 说明 |
| --- | --- | --- |
| body_temp | ℃ | 人体温度 |
| hr | bpm | 每分钟心跳次数 |
| spo2 | % | 血氧饱和度 |
| hr_valid | 0/1 | 心率数据是否有效 |
| spo2_valid | 0/1 | 血氧数据是否有效 |
| contact | 0/1/null | 可选推断字段，是否疑似有效接触 |

---

## 6. 状态码规范

所有预警字段统一采用四级编码：

| 状态码 | 含义 |
| --- | --- |
| 0 | 正常，无风险 |
| 1 | 预警，轻微异常，提醒注意 |
| 2 | 告警，明显风险，需要干预 |
| 3 | 紧急，高危状态，必须处理 |

### 6.1 人体温度预警 body_temp_alert

| 状态码 | 定义 |
| --- | --- |
| 0 | 人体温度正常 |
| 1 | 体温偏高或偏低，建议关注 |
| 2 | 明显异常，需要休息或复测 |
| 3 | 高危异常，需要立即处理 |

### 6.2 心率预警 hr_alert

| 状态码 | 定义 |
| --- | --- |
| 0 | 心率正常 |
| 1 | 心率轻微异常 |
| 2 | 心率明显异常 |
| 3 | 心率高危异常 |

### 6.3 血氧预警 spo2_alert

| 状态码 | 定义 |
| --- | --- |
| 0 | 血氧正常 |
| 1 | 血氧偏低，建议关注 |
| 2 | 血氧明显偏低，需要干预 |
| 3 | 血氧高危偏低，需要立即处理 |

其余 v1.1 状态码保持不变：

| 字段 | 说明 |
| --- | --- |
| collision_alert | 碰撞预警 |
| heat_alert | 高温预警 |
| fatigue_alert | 疲劳预警 |
| sos_alert | SOS 预警 |
| radar_alert | 超声波/雷达预警 |

---

## 7. 异常与缺失值处理

### 7.1 传感器离线或数据无效

| 情形 | 处理方式 |
| --- | --- |
| 传感器未初始化/断开 | 对应字段填 `null` |
| GPS 未定位 | `lat`、`lon`、`alt`、`speed` 填 `null`，`fix` 填 `0` |
| 生命体征传感器接触不良 | 若已实现接触判断，`contact` 填 `0`；否则该字段可省略或填 `null` |
| PPG 信号质量差 | `hr_valid` / `spo2_valid` 填 `0`；HR 或 SpO2 为 `0` 时应按无效值处理 |
| 数值超出合理范围 | 填 `null`，不上报异常原始值 |

### 7.2 生命体征缺失值示例

```json
{
  "vital": {
    "body_temp": null,
    "body_temp_source": "skin",
    "hr": null,
    "spo2": null,
    "hr_valid": 0,
    "spo2_valid": 0,
    "contact": null
  }
}
```

### 7.3 设备异常上报

当设备检测到自身硬件故障时，通过 `msg_type: "event"` 上报：

```json
{
  "version": "1.2",
  "device_id": "HLM-00123",
  "msg_type": "event",
  "timestamp": 1713600100,
  "event": "sensor_error",
  "sensor": "heart_rate",
  "detail": "PPG signal too weak"
}
```

---

## 8. 完整报文示例

### 8.1 常规数据包

Topic：

```text
helmet/HLM-00123/data
```

Payload：

```json
{
  "version": "1.2",
  "device_id": "HLM-00123",
  "msg_type": "data",
  "timestamp": 1713600000,
  "imu": {
    "ax": 0.01,
    "ay": -0.02,
    "az": 1.00,
    "gx": 0.30,
    "gy": -0.10,
    "gz": 0.05
  },
  "gps": {
    "lat": 31.23,
    "lon": 121.47,
    "alt": 12.50,
    "speed": 1.20,
    "course": 90.00,
    "sat": 8,
    "hdop": 1.20,
    "fix": 2
  },
  "env": {
    "temp": 32.50,
    "hum": 65.00,
    "light": 3200
  },
  "vital": {
    "body_temp": 36.80,
    "body_temp_source": "skin",
    "hr": 82,
    "spo2": 97,
    "contact": 1,
    "hr_valid": 1,
    "spo2_valid": 1
  },
  "device": {
    "battery": 85,
    "signal": 4,
    "work_time": 3600,
    "helmet_on": 1
  },
  "alerts": {
    "collision_alert": 0,
    "heat_alert": 1,
    "fatigue_alert": 0,
    "sos_alert": 0,
    "radar_alert": 0,
    "body_temp_alert": 0,
    "hr_alert": 0,
    "spo2_alert": 0
  }
}
```

### 8.2 心跳包

Topic：

```text
helmet/HLM-00123/heartbeat
```

Payload：

```json
{
  "version": "1.2",
  "device_id": "HLM-00123",
  "msg_type": "heartbeat",
  "timestamp": 1713600030,
  "battery": 85,
  "signal": 4,
  "helmet_on": 1
}
```

### 8.3 事件消息 - 血氧异常

Topic：

```text
helmet/HLM-00123/event
```

Payload：

```json
{
  "version": "1.2",
  "device_id": "HLM-00123",
  "msg_type": "event",
  "timestamp": 1713600120,
  "event": "low_spo2_detected",
  "spo2_alert": 2,
  "vital": {
    "hr": 108,
    "spo2": 89,
    "contact": 1,
    "hr_valid": 1,
    "spo2_valid": 1
  },
  "gps": {
    "lat": 31.23,
    "lon": 121.47,
    "fix": 2
  }
}
```

### 8.4 事件消息 - 人体温度异常

Topic：

```text
helmet/HLM-00123/event
```

Payload：

```json
{
  "version": "1.2",
  "device_id": "HLM-00123",
  "msg_type": "event",
  "timestamp": 1713600180,
  "event": "high_body_temp_detected",
  "body_temp_alert": 2,
  "vital": {
    "body_temp": 38.20,
    "body_temp_source": "skin",
    "contact": 1
  }
}
```

---

## 9. 版本兼容策略

| 规则 | 说明 |
| --- | --- |
| 向后兼容 | v1.2 只新增 `vital` 和健康预警字段，不删除或重命名 v1.1 字段 |
| 未知字段处理 | 接收方遇到未知字段应忽略，不报错 |
| 可选上传 | 未安装人体温度/心率/血氧传感器时，`vital` 对象可省略 |
| 版本号格式 | `主版本.次版本`，主版本变更表示不兼容结构变化 |
| 多版本并存 | 服务端根据 `version` 字段兼容处理不同版本报文 |

---

*文档版本：v1.2 | 最后更新：2026-05*
